# ParaCL Compiler

# Установка

```shell
git clone https://github.com/Maksim-Sebelev/ParaCL-Compiler.git;
cd ParaCL-Compiler;
cmake -S . -B build
cmake --build build
```

Использование:

```shell
./build/paracl-compiler <special json format>.ast.json -o <executable>
```
